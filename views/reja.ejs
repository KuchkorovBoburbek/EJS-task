<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reja</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />
  </head>
  <!-- <% %> bu sin dan foydalanib biz ejs da datalarni yoza olamiz -->
  <body style="background: #b8bbd1">
    <div class="container">
      <h1 class="display-3 text-center py-2">계획 및 목적 리스트</h1>

      <div
        class="jumbotron p-3 shadow-sm"
        style="
          background-color: white;
          border-radius: 8px;
        "
      >
        <form action="/create-item" id="create-form" method="POST">
          <div class="d-flex align-items-center" style="gap: 10px">
            <input
              type="text"
              placeholder="할 일을 입력하세요"
              id="create-fied"
              name="reja"
              autofocus
              autocomplete="off"
              class="form-control mr-3"
              type="text"
              style="flex: 1"
            />
            <button class="btn btn-primary" style="border-radius: 20px">
              새 계획 추가
            </button>
          </div>
        </form>
      </div>
      <!-- <%=items[0].reja %>     = ni qiymat qiymat qaytaradidan hollarda qoyamiz -->

      <!-- dbdagi har bir itemni olib kelib ui ga chiqarib berish uchun map dan foydalanamiz -->
       <ul id="list-item" class="list-group mb-3 mt-3">
      <% items.map((item)=>{%>
      
        <li
          class="list-group-item list-group-item-info d-flex align-items-center justify-content-between" 
        >
          <span class="item-text"><%= item.reja%></span>
           <div> <!-- endi har bir item id sini btnlarga biriktirib qpyamiz -->
            <button data-id="<%=item._id%>" class="edit-me btn btn-secondary" btn-sm mr-1>수정</button>
            <button data-id="<%=item._id%>" class="delete-me btn btn-danger" btn-sm>작제</button>
          </div>
        </li>
      
      <% })%>
      </ul>

      <button id="clean-all" class="btn btn-danger" style="border-radius: 20px">
        모든 계획 지우기
      </button>
    </div>

<script src="https://unpkg.com/axios/dist/axios.min.js">/* Bu request yuborish uchun 
Bu orqali yuborilgan req traditional emas
*/</script>

    <script src="/browser.js"> /*Biz bu yerda src ga public folderni korsatishimiz shart emas . Chunki kirgan userga ozi public folderga accsess boladi. Bu faqat frontend ga tegihali bolgani uchun u faqat frontenda run boldi backendga daxlli yoq*/ </script>
  </body>
</html>
